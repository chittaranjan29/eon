//Events:Events are nothing but action performed on the element of the webpage.
//Event Types: click, double click, drag, drop, on mouse over,on mouse out, on load, on unload, on key up, on key down etc.  

//alert() whenever a buton is clicked

function test()
{
    alert('button clicked');
}

